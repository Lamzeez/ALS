name: shared_services
description: Shared services for ALS-LMS (Auth, Supabase client, Sync, Storage)
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.5.0
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.8.0
  google_sign_in: ^6.2.2
  flutter_secure_storage: ^9.2.3
  local_auth: ^2.3.0
  connectivity_plus: ^6.1.1
  uuid: ^4.5.1
  path: ^1.9.0
  shared_models:
    path: ../shared_models

dev_dependencies:
  test: ^1.25.8
