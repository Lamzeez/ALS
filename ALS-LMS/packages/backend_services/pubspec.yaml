name: backend_services
description: Backend service wrappers (Supabase, Auth, Sync) for ALS-LMS
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.5.0
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.8.0
  google_sign_in: ^6.2.2
  flutter_secure_storage: ^9.2.3
  local_auth: ^2.3.0
  connectivity_plus: ^6.1.1
  flutter_dotenv: ^5.1.0
  uuid: ^4.5.1
  path: ^1.9.0
  sqflite: ^2.4.1
  path_provider: ^2.1.5
  shared_core:
    path: ../shared_core

dev_dependencies:
  test: ^1.25.8
