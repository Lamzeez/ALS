name: Flutter CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  analyze-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: 'stable'

      - name: Show Flutter
        run: flutter --version

      - name: Get dependencies (root package)
        run: |
          flutter pub get

      - name: Run tests for packages
        run: |
          set -e
          for pkg in ALS-LMS/packages/shared_core ALS-LMS/packages/backend_services ALS-LMS/packages/shared_ui ALS-LMS/apps/mobile_app ALS-LMS/apps/admin_web; do
            if [ -d "$pkg" ]; then
              echo "-- package: $pkg --"
              cd $pkg
              flutter pub get || true
              flutter test || true
              cd -
            fi
          done
